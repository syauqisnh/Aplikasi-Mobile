import 'package:flutter/material.dart';

class DataSyauqi extends StatelessWidget {
  const DataSyauqi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Daftar Pemain Tim', style: TextStyle(fontSize: 30),),
    );
  }
}